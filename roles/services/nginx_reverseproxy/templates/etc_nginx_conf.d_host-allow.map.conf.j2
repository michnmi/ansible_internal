# Generated by Ansible — do not edit manually.
# Allows only these Host headers. Everything else => $allow_host = 0
map $host $allow_host {
  default 0;
{% for h in reverseproxies | map(attribute='name') | unique | sort -%}
  {{ h }} 1;
{% endfor %}
}
